---
id: TASK-ABSHTRUE-657-ANALYSIS
type: task
task: ABSHTRUE-657
---

# Анализ

## Прочитанные документы

Отобраны по `code_paths` из `docs/_index.json`:

| Документ           | Почему релевантен                          |
| ------------------ | ------------------------------------------ |
| `PRODUCT-CONTESTS` | `backend/app/contest/**` попадает в diff    |

`ARCH-AUTH` **не читался** — не пересекается с затрагиваемым кодом.

## Текущее поведение

`backend/app/contest/service.py#ContestService.get_all` возвращает записи без
явного `ORDER BY`. Порядок определяется планом запроса, то есть непредсказуем.

Frontend в `frontend/src/pages/ContestsPage.vue` не задаёт начальную сортировку
таблицы и показывает данные в порядке ответа API.

## Что уточнили сверх ТЗ

- «Свежесть» = дата **начала приёма заявок**, не дата создания записи.
- Записи с пустой датой начала есть в проде (исторические импорты).

## Открытые вопросы

Нет. Намерение однозначно выводится из ТЗ и кода.
